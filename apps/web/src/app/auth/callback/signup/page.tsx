import { SignUpPageClient } from "@/components/auth/SignUpPageClient";

/** v1 / Bible: learner registration wizard (canonical URL). */
export default function AuthCallbackSignupPage() {
  return <SignUpPageClient />;
}
